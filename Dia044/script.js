const jogo = {
    nome: "Red Dead Redemption 2",
    ano: 2018,
    genero: "Ação e aventura",
    nota: 10
};


const propriedades = Object.keys(jogo);
const valores = Object.values(jogo);
const entradas = Object.entries(jogo);

console.log(propriedades);
console.log(valores);
console.log(entradas);