const linguagens = ["JavaScript", "Java", "Python", "C#", "PHP"];

for (const linguagem of linguagens) {
    console.log("Linguagem: " + linguagem);
}


const numeros = [10, 20, 30, 40, 50];

for (const n of numeros) {
    console.log(n * 2);
}